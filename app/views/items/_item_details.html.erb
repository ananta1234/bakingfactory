<h3><%= @item.name %></h3>

<% unless @item.photo_url.nil? %>
    <p><%= image_tag @item.photo_url, :class => "style_image", :align => "left" %></p>
<% end %>

<p>
  <b>Price</b>: <%= number_to_currency @item.current_price %> (<%= @item.units_per_item %> unit)
</p>

<p>
  <b>Weight</b>: <%= @item.weight %>
</p>

<p>
  <b>Active</b>: <%= @item.active ? 'Yes' : 'No' %>
</p>

<p>
  <em>An item at the Baking Factory since <%= @item.created_at.year %>.</em>
</p>

<p>
  <b>Description</b>: <%= @item.description %>
</p>