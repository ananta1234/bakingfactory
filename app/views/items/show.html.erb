<% if logged_in? && ( current_user.role?(:admin)) %>

  	<%= render partial: "partials/structure1", locals: {model_name: "item", 
                                                         primary: "item_details", 
                                                         secondary: nil,
                                                         sidebar: "item_price_history"} %>

	<%= render partial: "partials/item_options", locals: {model_name: "item", object: @item}  %>

<% else %>

  	<%= render partial: "partials/structure1", locals: {model_name: "item", 
                                                         primary: "item_details", 
                                                         secondary: nil,
                                                         sidebar: "similar_items"} %>
	<!-- Add buttons to list all, edit project or destroy object -->
	<%= render partial: "partials/cust_object_options", locals: {model_name: "item", object: @item}  %>

<% end %>


