<% if logged_in? && ( current_user.role?(:admin) || current_user.role?(:shipper)) %>

<%= render partial: "partials/index_structure", locals: {model_name: "address", 
                                                         primary: "tabbed_index", 
                                                         secondary: nil, 
                                                         sidebar: nil} %>
<% else %>

<%= render partial: "partials/index_structure", locals: {model_name: "address", 
                                                         primary: "address_list_customer", 
                                                         secondary: nil, 
                                                         sidebar: nil} %>

<% end %>